# ============================================================
#  Configuración del Bot de Discord para ESP32-CAM
#
#  INSTRUCCIONES:
#  1. Copia este archivo:   cp .env.example .env
#  2. Edita el .env con tus valores reales.
#  3. O usa el menú:        python main.py → opción 1
#
#  El archivo .env NUNCA debe subirse a Git (está en .gitignore).
# ============================================================

# ----------------------------------------------------------
# DISCORD — Token del bot (obligatorio)
# ----------------------------------------------------------
# Obtenlo en: https://discord.com/developers/applications
#   → Tu aplicación → Bot → Reset Token
DISCORD_TOKEN=TU_TOKEN_AQUI

# ----------------------------------------------------------
# ESP32-CAM — Dirección del servidor web
# ----------------------------------------------------------
# IP local de la cámara en tu red WiFi.
# La puedes ver en el monitor serie del Arduino IDE al encender la cámara,
# o en la lista de dispositivos conectados de tu router.
ESP32_IP=192.168.1.100

# Puerto del servidor web de la ESP32-CAM (por defecto 80, no suele cambiar).
ESP32_PORT=80

# ----------------------------------------------------------
# BOT — Prefix de comandos de texto (opcional)
# ----------------------------------------------------------
# Carácter(es) que se usan antes del nombre del comando en modo texto.
# Ejemplos: !foto  |  $foto  |  >>foto
# Los slash commands (/) siempre funcionan independientemente del prefix.
COMMAND_PREFIX=w!

# ----------------------------------------------------------
# BOT — Rol requerido para usar el bot (opcional)
# ----------------------------------------------------------
# ID del rol de Discord que debe tener un usuario para interactuar con el bot.
# 0 = sin restriccion (cualquier miembro puede usar el bot).
# Usa /rol @NombreDelRol en Discord para cambiar esto en tiempo real.
# Los administradores del servidor siempre tienen acceso sin importar este valor.
REQUIRED_ROLE_ID=0
