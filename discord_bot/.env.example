# ============================================================
#  Configuración del Bot de Discord para ESP32-CAM
#  Copia este archivo a ".env" y rellena los valores.
#  El archivo .env NUNCA debe subirse a Git (ya está en .gitignore).
# ============================================================

# ----------------------------------------------------------
# DISCORD — Obligatorio
# ----------------------------------------------------------

# Token del bot de Discord.
# Consíguelo en https://discord.com/developers/applications
# → Tu aplicación → Bot → Reset Token
DISCORD_TOKEN=tu_token_de_discord_aqui

# ----------------------------------------------------------
# ESP32-CAM — Dirección del servidor web de la cámara
# ----------------------------------------------------------

# IP local de la ESP32-CAM en tu red WiFi.
# Puedes verla en el monitor serie del Arduino IDE al encender la cámara,
# o desde el router en la lista de dispositivos conectados.
ESP32_IP=192.168.1.100

# Puerto del servidor web de la ESP32-CAM (por defecto 80).
ESP32_PORT=80

# ----------------------------------------------------------
# BOT — Opcional
# ----------------------------------------------------------

# Prefijo para comandos de texto (además de los slash commands /).
# Por defecto es "!" → !foto, !estado, etc.
COMMAND_PREFIX=!
